// SPDX-License-Identifier: MPL-2.0
= test/rules/ — Rules Engine Tests

ExUnit tests specific to the `lib/rules/` modules.
Currently contains `proof_strategy_selection_test.exs`;
additional rule tests live in `test/` (flat structure).
