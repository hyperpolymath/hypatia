# SPDX-License-Identifier: PMPL-1.0-or-later
# This workflow is managed by gh actions-lock.
name: OSSF Scorecard

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  id-token: write
  security-events: write

jobs:
  scorecard:
    uses: hyperpolymath/standards/.github/workflows/scorecard-reusable.yml@d5fe075a50ab3ce4f41614d66ed77f152fda134f
    permissions:
      actions: read
      contents: read
      security-events: write
      id-token: write
    secrets: inherit
