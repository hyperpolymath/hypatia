# SPDX-License-Identifier: PLMP-1.0-or-later
# cicd-hyper-a Kubernetes Ingress
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: cicd-hyper-a-ingress
  namespace: cicd-hyper-a
  labels:
    app.kubernetes.io/name: cicd-hyper-a
    app.kubernetes.io/component: ingress
    app.kubernetes.io/part-of: cicd-hyper-a
  annotations:
    # Ingress controller annotations (nginx-ingress)
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "300"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "300"

    # Rate limiting
    nginx.ingress.kubernetes.io/limit-rps: "100"
    nginx.ingress.kubernetes.io/limit-connections: "50"

    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      add_header X-Frame-Options "SAMEORIGIN" always;
      add_header X-Content-Type-Options "nosniff" always;
      add_header X-XSS-Protection "1; mode=block" always;
      add_header Referrer-Policy "strict-origin-when-cross-origin" always;
      add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline';" always;

    # cert-manager for automatic TLS certificates
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - cicd-hyper-a.example.com        # TODO: Replace with your domain
        - api.cicd-hyper-a.example.com
        - registry.cicd-hyper-a.example.com
        - grafana.cicd-hyper-a.example.com
      secretName: cicd-hyper-a-tls
  rules:
    # Main API endpoint
    - host: cicd-hyper-a.example.com      # TODO: Replace with your domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 8000

    # Explicit API subdomain
    - host: api.cicd-hyper-a.example.com  # TODO: Replace with your domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: api
                port:
                  number: 8000

    # Registry endpoint (for ruleset operations)
    - host: registry.cicd-hyper-a.example.com  # TODO: Replace with your domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: registry
                port:
                  number: 8080

    # Grafana monitoring dashboard
    - host: grafana.cicd-hyper-a.example.com  # TODO: Replace with your domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 3000
---
# ClusterIssuer for Let's Encrypt (requires cert-manager)
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: security@hyperpolymath.dev
    privateKeySecretRef:
      name: letsencrypt-prod-account-key
    solvers:
      - http01:
          ingress:
            class: nginx
---
# Certificate resource (alternative to annotations)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: cicd-hyper-a-tls
  namespace: cicd-hyper-a
spec:
  secretName: cicd-hyper-a-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: cicd-hyper-a.example.com  # TODO: Replace with your domain
  dnsNames:
    - cicd-hyper-a.example.com
    - api.cicd-hyper-a.example.com
    - registry.cicd-hyper-a.example.com
    - grafana.cicd-hyper-a.example.com
